import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: const Text("Multiple Images")),
        body: ListView(
          children: [
            Image.asset('assets/images/THAR.jpg'),
            Image.asset('assets/images/r15.jpg'),
          ],
        ),
      ),
    );
  }
}
